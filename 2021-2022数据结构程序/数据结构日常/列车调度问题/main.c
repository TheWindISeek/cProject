#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//我现在的需求是3个数组，1 2 3，其中二数组就是存放结果，1数组是放原来的数据，3数组是栈，后进先出，一数组的指针后移，二数组也是
int main()
{
char a[27];
char b[27];
char c[27];
gets(a);
gets(b);
//printf("%c",a[0]);
//printf("%c",b[0]);

    return 0;
}
